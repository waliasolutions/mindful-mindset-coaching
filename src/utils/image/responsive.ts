
/**
 * Generate a responsive image srcSet 
 * This is a simplified version that doesn't depend on Sharp
 */
export const generateSrcSet = (src: string): string => {
  // Return empty string since we're not using Sharp to generate different sizes
  return '';
};
